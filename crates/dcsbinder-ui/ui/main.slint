// Placeholder Slint file. Real UI surfaces land in M4 — see docs/ARCHITECTURE.md
// for the six-screen design (Dashboard / Devices / Conflicts / Diff / History / Settings).

export component App inherits Window {
    title: "DCSBinder";
    width: 800px;
    height: 600px;
    Text {
        text: "DCSBinder — UI lands in M4";
        font-size: 16px;
    }
}
