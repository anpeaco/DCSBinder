//! Group scanned files by `(install_root, aircraft, subtype, normalized_device_name)`
//! and surface groups with >1 distinct GUID as conflicts.
//!
//! No automatic fuzzy matching in v1 — see the alias map in `config`.
//!
//! Until M1 lands, this module is a stub.
